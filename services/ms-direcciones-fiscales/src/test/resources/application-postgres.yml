spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/direcciones_fiscales
    username: labuser
    password: labpass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

fideicomisos-adapter:
  base-url: http://localhost:9999
